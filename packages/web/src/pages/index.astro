---
const features = [
  {
    icon: "zap",
    title: "0.5s startup",
    description: "Your thoughts shouldn't wait",
  },
  {
    icon: "keyboard",
    title: "Vim + Twitter keybindings",
    description: "hjkl navigation, never touch your mouse",
  },
  {
    icon: "layers",
    title: "Cards, not documents",
    description: "Think in fragments, not paragraphs",
  },
  {
    icon: "sparkles",
    title: "AI synthesis",
    description: "Scattered ideas → polished drafts",
  },
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <title>Jot Deck — Jot Down. Shape Up.</title>
    <meta
      name="description"
      content="A blazing-fast, keyboard-driven note app for power users. Stack your thoughts as cards, navigate with Vim keys, and let AI crystallize your fragments into polished text."
    />
    <meta property="og:title" content="Jot Deck — Jot Down. Shape Up." />
    <meta
      property="og:description"
      content="A blazing-fast, keyboard-driven note app for power users."
    />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://jot-deck.com" />
    <meta property="og:image" content="https://jot-deck.com/ogp.png" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:image" content="https://jot-deck.com/ogp.png" />
  </head>
  <body>
    <main>
      <section class="hero">
        <div class="container">
          <p class="tagline">Jot Down. Shape Up.</p>
          <h1>Jot Deck</h1>
          <p class="hook">
            <strong>Tired of waiting for Notion to load?</strong><br />
            <strong>Or getting lost in deep nested bullet lists?</strong>
          </p>
          <p class="description">
            Jot Deck is a local-first note app built for speed obsessives.
            Capture ideas as flat, tweet-sized cards in a TweetDeck-style
            multi-column layout — no hierarchy, no lag, just flow.
          </p>
          <form
            class="waitlist-form"
            action="https://formspree.io/f/mqeezpak"
            method="POST"
          >
            <input
              type="email"
              name="email"
              placeholder="you@example.com"
              required
            />
            <button type="submit" class="button primary">Join Waitlist</button>
          </form>
          <p class="waitlist-note">Be the first to know when we launch.</p>
        </div>
      </section>

      <section class="screenshot">
        <div class="container">
          <img
            src="/app-screenshot.png"
            alt="Jot Deck app showing multi-column layout with cards"
            width="1200"
            height="832"
          />
        </div>
      </section>

      <section id="features" class="features">
        <div class="container">
          <h2>Why Jot Deck?</h2>
          <div class="feature-grid">
            {
              features.map((feature) => (
                <div class="feature-card">
                  <div class="feature-icon" data-icon={feature.icon} />
                  <h3>{feature.title}</h3>
                  <p>{feature.description}</p>
                </div>
              ))
            }
          </div>
        </div>
      </section>

      <section class="tech">
        <div class="container">
          <p class="tech-stack">
            Built with <strong>Tauri + Rust</strong> for native performance.<br
            />
            Offline-first with <strong>SQLite</strong>.
          </p>
        </div>
      </section>

      <section id="waitlist" class="waitlist">
        <div class="container">
          <h2>Join the Waitlist</h2>
          <p>Launching soon for Windows. macOS and Linux to follow.</p>
          <form
            class="waitlist-form"
            action="https://formspree.io/f/mqeezpak"
            method="POST"
          >
            <input
              type="email"
              name="email"
              placeholder="you@example.com"
              required
            />
            <button type="submit" class="button primary">Notify Me</button>
          </form>
        </div>
      </section>

      <footer>
        <div class="container">
          <p>&copy; 2025 Jot Deck. All rights reserved.</p>
        </div>
      </footer>
    </main>

    <style>
      :root {
        --color-bg: #0a0a0a;
        --color-surface: #141414;
        --color-border: #262626;
        --color-text: #fafafa;
        --color-text-muted: #a1a1aa;
        --color-primary: #3b82f6;
        --color-primary-hover: #2563eb;
        --font-sans: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, sans-serif;
        --font-mono: ui-monospace, SFMono-Regular, "SF Mono", Menlo, monospace;
      }

      @media (prefers-color-scheme: light) {
        :root {
          --color-bg: #fafafa;
          --color-surface: #ffffff;
          --color-border: #e4e4e7;
          --color-text: #09090b;
          --color-text-muted: #71717a;
        }
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      html {
        scroll-behavior: smooth;
      }

      body {
        font-family: var(--font-sans);
        background: var(--color-bg);
        color: var(--color-text);
        line-height: 1.6;
      }

      .container {
        max-width: 1024px;
        margin: 0 auto;
        padding: 0 1.5rem;
      }

      /* Hero */
      .hero {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        padding: 4rem 0;
      }

      .tagline {
        font-family: var(--font-mono);
        font-size: 0.875rem;
        color: var(--color-primary);
        text-transform: uppercase;
        letter-spacing: 0.1em;
        margin-bottom: 1rem;
      }

      .hero h1 {
        font-size: clamp(3rem, 10vw, 6rem);
        font-weight: 800;
        letter-spacing: -0.02em;
        margin-bottom: 2rem;
      }

      .hook {
        font-size: 1.25rem;
        color: var(--color-text-muted);
        margin-bottom: 1.5rem;
      }

      .hook strong {
        color: var(--color-text);
      }

      .description {
        font-size: 1.125rem;
        color: var(--color-text-muted);
        max-width: 600px;
        margin: 0 auto 2.5rem;
      }

      .waitlist-form {
        display: flex;
        gap: 0.5rem;
        justify-content: center;
        flex-wrap: wrap;
        max-width: 400px;
        margin: 0 auto;
      }

      .waitlist-form input[type="email"] {
        flex: 1;
        min-width: 200px;
        padding: 0.875rem 1rem;
        font-size: 1rem;
        border: 1px solid var(--color-border);
        border-radius: 8px;
        background: var(--color-surface);
        color: var(--color-text);
      }

      .waitlist-form input[type="email"]::placeholder {
        color: var(--color-text-muted);
      }

      .waitlist-form input[type="email"]:focus {
        outline: none;
        border-color: var(--color-primary);
      }

      .waitlist-note {
        margin-top: 1rem;
        font-size: 0.875rem;
        color: var(--color-text-muted);
      }

      .button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 0.875rem 1.75rem;
        font-size: 1rem;
        font-weight: 500;
        text-decoration: none;
        border-radius: 8px;
        transition: all 0.15s ease;
      }

      .button.primary {
        background: var(--color-primary);
        color: white;
      }

      .button.primary:hover {
        background: var(--color-primary-hover);
      }

      button.button {
        border: none;
        cursor: pointer;
      }

      .button.secondary {
        background: var(--color-surface);
        color: var(--color-text);
        border: 1px solid var(--color-border);
      }

      .button.secondary:hover {
        border-color: var(--color-text-muted);
      }

      /* Screenshot */
      .screenshot {
        padding: 0 0 4rem;
      }

      .screenshot img {
        width: 100%;
        height: auto;
        border-radius: 12px;
        border: 1px solid var(--color-border);
        box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.4);
      }

      /* Features */
      .features {
        padding: 6rem 0;
        background: var(--color-surface);
      }

      .features h2 {
        font-size: 2rem;
        text-align: center;
        margin-bottom: 3rem;
      }

      .feature-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
        gap: 2rem;
      }

      .feature-card {
        padding: 1.5rem;
        background: var(--color-bg);
        border: 1px solid var(--color-border);
        border-radius: 12px;
      }

      .feature-icon {
        width: 48px;
        height: 48px;
        background: var(--color-primary);
        border-radius: 10px;
        margin-bottom: 1rem;
        opacity: 0.9;
      }

      .feature-card h3 {
        font-size: 1.125rem;
        margin-bottom: 0.5rem;
      }

      .feature-card p {
        font-size: 0.9375rem;
        color: var(--color-text-muted);
      }

      /* Tech */
      .tech {
        padding: 4rem 0;
        text-align: center;
      }

      .tech-stack {
        font-family: var(--font-mono);
        font-size: 0.9375rem;
        color: var(--color-text-muted);
      }

      .tech-stack strong {
        color: var(--color-text);
      }

      /* Waitlist */
      .waitlist {
        padding: 6rem 0;
        text-align: center;
        background: var(--color-surface);
      }

      .waitlist h2 {
        font-size: 2rem;
        margin-bottom: 1rem;
      }

      .waitlist p {
        color: var(--color-text-muted);
        margin-bottom: 2rem;
      }

      /* Footer */
      footer {
        padding: 2rem 0;
        text-align: center;
        border-top: 1px solid var(--color-border);
      }

      footer p {
        font-size: 0.875rem;
        color: var(--color-text-muted);
      }
    </style>
  </body>
</html>
